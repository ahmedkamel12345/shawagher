# التحديث من الموبايل — شواغر

الفكرة: الموقع يحدّث نفسه بنفسه على GitHub، وإنت من الموبايل تعمل حاجتين بس:
إضافة إعلانات واتساب، أو الضغط على زر تشغيل يدوي.

---

## التجهيز (مرة واحدة، من الكمبيوتر — ١٥ دقيقة)

### ١) ارفع المشروع على GitHub
- اعمل حساب على github.com
- New repository → الاسم `shawagher` → **Private** → Create
- في صفحة المستودع: `uploading an existing file` → اسحب مجلد المشروع كله
  (لازم يحتوي: `collector/` و `web/` و `.github/workflows/collect.yml`)

### ٢) حط المفاتيح
Settings ← Secrets and variables ← Actions ← **New repository secret**

| الاسم | القيمة |
|---|---|
| `CAREERJET_API_KEY` | مفتاح Careerjet |
| `JOOBLE_KEY` | مفتاح Jooble |

(لو مفتاح لسه ماوصلش، سيبه — السكريبت بيتخطى المصدر ويكمّل)

### ٣) اربط Netlify بالمستودع
- في Netlify: Add new site ← **Import an existing project** ← GitHub ← اختار `shawagher`
- **Publish directory:** `web`
- Build command: اتركه فاضي
- Deploy

من دلوقتي أي تغيير في GitHub بينشر على الموقع لوحده خلال دقيقة.

### ٤) شغّل التجميع أول مرة
تبويب **Actions** ← "تجميع الشواغر" ← **Run workflow**

---

## الاستخدام اليومي من الموبايل

### أ) إضافة إعلان واتساب
1. انسخ الإعلان من الجروب
2. افتح `موقعك/whatsapp-import.html`
3. الصق ← **حلّل النص** ← راجع وعدّل ← **انسخ JSON**
4. افتح تطبيق GitHub (أو الموقع من المتصفح) ← `web/data/manual.json`
   ← أيقونة القلم ✏️ ← امسح المحتوى ← الصق ← **Commit changes**
5. خلاص. الوظيفة تظهر على الموقع خلال دقيقتين.

### ب) تحديث فوري للشواغر
تطبيق GitHub ← Actions ← تجميع الشواغر ← **Run workflow**

### ج) إضافة جروب واتساب للدليل
نفس الطريقة، بس الملف `web/data/groups.json`

---

## ملاحظات
- التجميع بيشتغل تلقائي كل ٦ ساعات من غير أي تدخل منك.
- إعلانات واتساب محفوظة في `collector/manual_store.json` ومش بتضيع مع التحديث.
- لو حصل خطأ في التشغيل، هيوصلك إيميل من GitHub وتلاقي التفاصيل في تبويب Actions.
